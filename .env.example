# Base path for deployment (e.g. '/' for Vercel/Netlify or custom domain, './' or '/portfolio/' for GitHub Pages)
VITE_BASE_PATH=/

# Optional GitHub API token to raise client-side rate limits from 60 to 5000 requests/hour
# Never commit real private keys or tokens into source control
VITE_GITHUB_TOKEN=
